@model CustomerApp.Models.Cart

@Html.ActionLink("View Cart", "Index", "Cart",
    new { returnUrl = Request.Url.PathAndQuery})

<li class="navbar-text navbar-right">
    <b>Your cart:</b>
    @Model.Lines.Sum(x => x.Quantity) items,
    @Model.ComputeTotalValue().ToString("c")
</li>